const API_ENDPOINTS = {
   //todo -> first make backend then use these endpoints
    sample: '/sample',
    

    /**User Endpoint */
    addUser : '/users/add-user',
  };
  
  export default API_ENDPOINTS;
  